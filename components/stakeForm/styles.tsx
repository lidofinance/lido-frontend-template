import styled from 'styled-components';

export const InputWrapper = styled.div`
  margin-bottom: ${({ theme }) => theme.spaceMap.md}px;
`;

export const PseudoFormWrapper = styled.div`
  margin-bottom: 24px;
`;
